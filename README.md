# Husni Sarafi Portfolio

Animated editorial portfolio built as a React 18 + Vite single-page app with inline Remotion Player sections.

## Setup

```bash
npm install
npm run dev
```

The dev server runs on `http://127.0.0.1:5173` by default.

## Scripts

```bash
npm run build
npm run preview
npm run remotion
```
